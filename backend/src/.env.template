SECRET_KEY="top_secret"
ENVIRONMENT="dev"

# --- LOCAL DEVELOPMENT (Current) ---
FIRESTORE_EMULATOR_HOST="127.0.0.1:8080"
GOOGLE_CLOUD_PROJECT="demo-local"

# --- PRODUCTION (Future - Cloud Run) ---
# In Cloud Run, you simply DELETE the FIRESTORE_EMULATOR_HOST line.
# Google will automatically provide the Project ID.